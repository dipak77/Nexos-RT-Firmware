// PlatformIO src_dir is `main` so the IDF product still builds from CMake.
// Arduino compiles this file, which pulls in the dual-kernel dashboard.
#include "../src/kernel_manager.cpp"
#include "../src/main.cpp"
